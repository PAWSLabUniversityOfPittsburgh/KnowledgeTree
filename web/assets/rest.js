// JavaScript Documentopener={	// CSS classes	openerElementName:'opener',	openerControlElementName:'openerControl',	openerPrefix:'opnS', /* opener subordinate*/	openerControlPrefix:'opn', /* opener control itself*/	assetsPath:'',	init:function(_assetsPath)	{		/* Check for DOM and apply a class to the body if it is supported */		if(!document.getElementById || !document.getElementsByName){return;}				opener.assetsPath = _assetsPath; 		/*alert('start0 ' + opener.assetsPath);/**/		/* modify all the openers */		var openers = document.getElementsByName(opener.openerElementName); 		var openControls = document.getElementsByName(opener.openerControlElementName); 		if(openers != null && openControls != null && openers.length == openControls.length)		{			for(var i=0; i<openers.length; i++)			{				/* close the openers*/				openers[i].style.display='none';				/* assign Ids to openers and contorls*/				openers[i].id = i + opener.openerPrefix;				openControls[i].id = i + opener.openerControlPrefix;				/* assign opener function */				openControls[i].onclick = function()					{						opener.flipOpener(this); /* opener control gets a reference to itself*/					}				/* insert icons */				openControls[i].innerHTML = '<img src="' + opener.assetsPath + 'dir_plus.gif">&nbsp;List';			}		}	},		flipOpener:function(o /* openec control element */)	{		if(!document.getElementById || !document.getElementsByName){return;}		var _opener = document.getElementById(o.id+'S');		var closed = _opener.style.display == 'none';		_opener.style.display = (closed)?'block':'none';/**/		o.innerHTML = '<img src="' + opener.assetsPath + ((!closed)?'dir_plus.gif':'dir_minus.gif') + '">&nbsp;List';	}}